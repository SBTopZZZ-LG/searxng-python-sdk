name: CI

on:
  push:
    branches:
      - main
  pull_request:

permissions:
  contents: read
  pull-requests: write

jobs:
  ci:
    name: CI
    uses: ./.github/workflows/lint-and-test.yml
    with:
      python-version: "3.11"
      searxng-image: "searxng/searxng:2026.5.10-df1f24fb7"

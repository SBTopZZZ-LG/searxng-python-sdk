repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.15.12
    hooks:
      - id: ruff
        args: [--fix, --config=pyproject.toml, --target-version=py39]
      - id: ruff-format
        args: [--config=pyproject.toml, --target-version=py39]
